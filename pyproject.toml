[project]
name = "open-multi-agent"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["app", "core", "mcp_local"]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest-asyncio>=1.3.0",
]
